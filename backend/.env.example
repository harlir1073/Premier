# Server
PORT=4000
JWT_SECRET=change_this_to_a_long_random_string

# Database (Cloud SQL / PostgreSQL)
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=backoffice
DB_USER=postgres
DB_PASSWORD=postgres

# If connecting via Cloud SQL connector / SSL, adjust db.js accordingly
